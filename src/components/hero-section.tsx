import { ProblemGround } from "./problem-ground";

export const Hero = () => {
    return (
        <section className="w-full">
            <h2 className="mb-[56px] bg-clip-text font-bold bg-gradient-to-b text-transparent from-gradient_start to-gradient_stop text-[32px]">
                Relations and Functions (Mathematics)
            </h2>
            <ProblemGround />
        </section>
    );
};
